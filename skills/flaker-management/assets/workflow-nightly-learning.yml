name: flaker-nightly-learning

on:
  schedule:
    - cron: "0 18 * * *"
  workflow_dispatch:

jobs:
  flaker-nightly:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        with:
          version: 10

      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: pnpm

      - run: pnpm install --frozen-lockfile
      - run: mkdir -p .artifacts
      - run: pnpm flaker import --ci --days 1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      - run: pnpm flaker run --gate release
      - run: pnpm flaker status --list flaky --json > .artifacts/flaky-list.json
      - run: pnpm flaker status --markdown --window-days 7 > .artifacts/flaker-review.md

      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: flaker-nightly-review
          path: .artifacts/
