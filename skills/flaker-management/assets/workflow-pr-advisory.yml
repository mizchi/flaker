name: flaker-pr-advisory

on:
  pull_request:

jobs:
  flaker-advisory:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        with:
          version: 10

      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: pnpm

      - run: pnpm install --frozen-lockfile
      - run: mkdir -p .artifacts
      - run: pnpm flaker run --gate merge
        continue-on-error: true

      - run: pnpm flaker status --markdown > .artifacts/kpi.md
      - run: pnpm flaker status --json > .artifacts/flaker-eval.json

      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: flaker-pr-advisory
          path: .artifacts/
